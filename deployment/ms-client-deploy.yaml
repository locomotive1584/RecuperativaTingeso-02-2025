apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-microservice-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client-microservice-deployment
  template:
    metadata:
      labels:
        app: client-microservice-deployment
    spec:
      containers:
        - name: client-microservice-container
          image: locomotive1584/client-microservice:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_CLOUD_CONFIG_URI
              value: http://config:8888
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://eureka:8761/eureka
            - name: eureka.instance.preferIpAddress
              value: "true"
            - name: POSTGRES_DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: postgres-config-map
                  key: POSTGRES_DB_HOST
            - name: POSTGRES_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: postgres-config-map
                  key: POSTGRES_DB_CLIENT
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: client-microservice
spec:
  selector:
    app: client-microservice-deployment
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP