server:
  port: 8003
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/report-db
    username: ${DB_USER:postgres}
    password: ${DB_PASS:123}
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    enabled: false